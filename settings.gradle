include "osgi-bundle-spring-scanner"
include "osgi-spring-extender"

rootProject.name = 'example-spring-boot-embedded-felix'
rootProject.children.each {project ->
	project.buildFileName = "${project.name}.gradle"
}
